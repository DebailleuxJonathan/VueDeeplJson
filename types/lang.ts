export interface Languages {
    language: string
    name: string
    supports_formality: boolean
}

export interface TextareaLanguageConfigs {
    isLoaded: boolean
    lang: any
    text: string
}
